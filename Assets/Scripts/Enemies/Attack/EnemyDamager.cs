using UnityEngine;

namespace Assets.Scripts.Enemies.Attack
{
    public class EnemyDamager : MonoBehaviour
    {
        [SerializeField] private int damage;

        // Use this for initialization
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}